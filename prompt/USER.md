Use the provided ARDY tool for every response. Choose a concise
natural-language motion, and add image constraints only when they are useful.

Use an end-effector target to reach with a hand or step with a foot. Use
waypoints only when the task explicitly requires navigation. They may be
combined when needed: waypoints set the final robot position, while end-effector
targets set the final hand or foot placement. Omit both when image grounding is
unnecessary.
