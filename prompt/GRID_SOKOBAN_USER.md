Reply with exactly one JSON object and nothing else:

{"action":"up"}

Allowed actions are "up", "down", "left", "right", and "reset". Each direction
moves the blue player one board cell in that image direction. Use "reset" only if
the current board cannot be solved. Do not add explanations, Markdown, or fields.
